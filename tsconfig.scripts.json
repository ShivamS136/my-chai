{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.scripts.tsbuildinfo",
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    // read-config.mts (reached via check-placeholder.mjs) uses `import.meta.url`,
    // which needs no extra lib; node types cover node:fs and the URL global.
    "types": ["node"],

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "allowJs": true,
    "checkJs": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "allowImportingTsExtensions": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "//": "Explicit file list, never a scripts/** glob: scripts/posthog-dashboard.mjs predates this setup and is not JSDoc-typed, so checkJs would red-light it. placeholder-detect.test.ts lives in THIS project rather than the app one because it imports a JSDoc-typed .mjs, which needs allowJs to resolve — without it the test belonged to no project and was silently never typechecked.",
  "include": [
    "scripts/check-placeholder.mjs",
    "scripts/placeholder-detect.mjs",
    "scripts/placeholder-detect.test.ts"
  ]
}
